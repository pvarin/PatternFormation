function f = F(mu,V)
    f = exp(mu+V)-V;
end

function L = laplacianMatrix(N, dx)
    uDiag = [0; ones(N-1,1)];
    cDiag = [-1;-2*ones(N-2,1);-1];
    lDiag = [ones(N-1,1); 0];
    B = [uDiag, cDiag, lDiag];
    d = -1:1;
    L = spdiags(B, d, N, N)/dx^2;
end