v0 = 1;
kappa = 1;
dx = .02;
mu = -3;

[u v] = full_chemIVPShoot(1,1,dx,mu);

N = length(u);
L = dx*N*2;
mass = mean(u);


guess = [u;v;0];
bvp = full_ChemBvp(kappa,N);
sol = fsolve(@(sol) bvp(sol,mass,L),guess);

plot(bvp(sol,mass,L))
U = sol(1:(end-1)/2);
V = sol(1:(end+1)/2);
sigma = sol(end);