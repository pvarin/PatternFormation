function D = derivativeMatrix(N,dx,bc)
    E = ones(N,1);
    D = spdiags([-E E],[-1 1],N,N);
    
    switch upper(bc)
        case 'NEUMANN'
            
        case 'DIRICHLET'
        case 'PERIODIC'
    end
end