function L = laplacianMatrix(N,dx,bc)
    E = ones(N,1);
    L = spdiags([E -2*E E],-1:1,N,N);
    
    switch upper(bc)
        case 'NEUMAN'
            L()
        case 'DIRICHLET'
        case 'PERIODIC'
        otherwise
    end
end